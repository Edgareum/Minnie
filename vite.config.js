import { defineConfig } from 'vite'

export default defineConfig({
  // Base path for GitHub Pages: https://edgareum.github.io/Minnie/
  base: '/Minnie/',
  // Serve static assets from the public/ folder (images)
  publicDir: 'public',
})
